name: ipboard3_viewer
description: Navigate through an IPBoard 3 Database
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  expandable:
  flutter:
    sdk: flutter
  flutter_html:
  go_router:
  html_unescape:
  intl:
  mysql1:
  scroll_to_index:
  url_launcher:

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/config/
